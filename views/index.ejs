<%-include('header'); -%>

    <main>
        <h1>Bonjour</h1>

        <% user.forEach(function(element){ %>
            <%= element.login %>
            <% }); %>
    </main>

<%- include('footer');  -%>

